# Project Context — Hotel PMS

Primary stack:
- Java + Spring Boot microservices
- React / TypeScript frontend
- PostgreSQL
- Redis
- Docker + Docker Compose
- Flyway
- YAML / environment configuration / shell

Known operational services include frontend, api-gateway, auth-service,
frontdesk-service, guest-service, billing-service, fb-service,
notification-service, config-service/config-server, postgres and redis.

Production-real behavior only. Do not use mocks/fake integrations to claim completion.
Existing data and uncommitted work must be preserved.
